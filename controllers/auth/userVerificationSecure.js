// importing required models
const mongoose = require("mongoose");

// importing required models
const OTPCollection = require('../../models/client/otpCollection');

// otp verification method
module.exports.otpVerification = (req, res, next) => {

    

};